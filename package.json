{
  "name": "mojito-site-checks",
  "private": true,
  "description": "Dev-only deps for the pre-publish check workflow. The site itself ships zero JS deps.",
  "scripts": {
    "check": "./scripts/check.sh"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "lighthouse": "^12.2.1",
    "stylelint": "^16.10.0",
    "stylelint-config-standard": "^36.0.1"
  }
}
